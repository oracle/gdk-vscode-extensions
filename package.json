{
	"devDependencies": {
		"@vscode/vsce": "^2.15.0"
	},
	"scripts": {
		"prebuild": "npm install",
		"build": "npm run build:micronaut & npm run build:gcn-database && npm run build:gcn",
		"build:gcn-database": "cd gcn-database-drivers; npm run build",
		"build:micronaut": "cd micronaut; npm run build",
		"build:gcn": "cd gcn; npm run build"
	}
}
